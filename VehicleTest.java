import org.junit.jupiter.api.Test;

import java.awt.*;

import static org.junit.jupiter.api.Assertions.*;

public class VehicleTest {

    @Test
    void testScania(){
        /*Scania sc = new Scania();
        assertEquals(0, sc.getCurrentSpeed());
        sc.gas(1);
        assertEquals(1, sc.getCurrentSpeed());
        sc.changeFleetAngle(10);
        sc.move();
        assertEquals(150, sc.getPosition().y);
        assertEquals(150, sc.getPosition().x);

*/
    }

    @Test
    void testMotherTruck(){
       /* MotherTruck mt = new MotherTruck();
        mt.gas(10);
        assertEquals(1, mt.getCurrentSpeed());
        mt.lowerFleet();
        mt.move();
        assertEquals(0, mt.getPoint().y);
        assertEquals(1, mt.getPoint().x);
        Vehicle testV = new Vehicle(2, 100, Color.BLACK, "TestVehicle", new Point(1,1));
        mt.brake(1);
        assertEquals(0, mt.getCurrentSpeed());
        mt.lowerFleet();
        mt.loadCar(testV);
        assertEquals(1, mt.getVehicles().size());*/
    }

    @Test
    void testBOAT(){/*
        MotherBoat motherBoat = new MotherBoat();
        motherBoat.gas(10);
        assertEquals(1, motherBoat.getCurrentSpeed());
        motherBoat.lowerFleet();
        motherBoat.move();
        assertEquals(0, motherBoat.getPoint().y);
        assertEquals(1, motherBoat.getPoint().x);
        Vehicle testV = new Vehicle(2, 100, Color.BLACK, "TestVehicle", new Point(1,1));
        motherBoat.brake(1);
        assertEquals(0, motherBoat.getCurrentSpeed());
        motherBoat.lowerFleet();
        motherBoat.loadCar(testV);
        assertEquals(1, motherBoat.getVechicles().size());*/
    }



}